# Apple Core compatibility feeds

This public repository preserves the updater URL used by older Apple Core installations. It contains public update metadata only. App source and credentials do not belong here. Downloads and the current signed feed are hosted at https://applecore.app.

Publish the exact signed appcast from the private release workflow to `apple-core/appcast.xml`. Do not edit signed XML after publication.
